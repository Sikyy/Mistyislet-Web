# Content Specification

This document contains the current website copy to implement.

## Global

Brand:

```text
Mistyislet
```

Primary CTA:

```text
Preview
```

Navigation:

```text
Product / Solutions / Resources / Partners / Pricing
```

## Hero

Heading:

```text
Every door, quietly in control.
```

Body:

```text
Cloud access control for readers, edge gateways, mobile credentials, and every place you operate.
```

CTA:

```text
Preview
```

## Product

Eyebrow:

```text
Product
```

Heading:

```text
Readers and edge gateways, built for every door.
```

Cards:

| Title | Body |
|---|---|
| Misty Reader | NFC, BLE, QR, and mobile credentials at the door, ready for modern access workflows. |
| Edge Gateway | Local decisions, offline cache, device heartbeat, and event sync for every connected door. |
| Credential Layer | Issue, revoke, and audit cards, mobile passes, QR codes, and access rules from the cloud. |

## Solutions

Eyebrow:

```text
Solutions
```

Heading:

```text
Cloud SaaS and mobile access, connected in one control plane.
```

Tabs/cards:

| Title | Body |
|---|---|
| Cloud SaaS | Centralize sites, doors, people, groups, schedules, credentials, and events in one browser-based console. |
| Mobile Access | Turn phones into secure credentials with NFC, BLE, QR, and time-bound passes for tenants, staff, and visitors. |
| Access Integrations | Connect identity, HR, visitor workflows, and building systems through APIs and event webhooks. |
| Visitor Access | Send temporary QR or mobile passes, set schedules, and audit every visit from invite to unlock. |

CTA:

```text
Preview
```

## How It Works

Eyebrow:

```text
How It Works
```

Heading:

```text
From access rule to door unlock in three steps.
```

Steps:

| Step | Title | Body |
|---|---|---|
| 1 | Configure | Create places, doors, users, groups, and time-based access rules in the cloud. |
| 2 | Present | Users unlock with mobile credentials, NFC cards, QR passes, or reader events at the door. |
| 3 | Audit | Gateways sync decisions and events back to the console for real-time review. |

## Benefits

Eyebrow:

```text
Benefits
```

Heading:

```text
A calmer control plane for physical access.
```

Cards:

| Title | Body |
|---|---|
| Mobile Credentials | Phone-first access without plastic-card bottlenecks. |
| Offline Ready | Gateways keep access decisions available when the network is unstable. |
| Real-time Revocation | Remove access instantly when roles, tenants, or risk changes. |
| Centralized Events | Search door events, credential use, and admin actions in one timeline. |
| Hardware Visibility | Monitor readers, gateways, and door health before issues reach users. |
| Built-in Trust | Least-privilege roles, audit trails, and secure credential lifecycle controls. |

## Partners

Eyebrow:

```text
Partners
```

Heading:

```text
Built with an ecosystem mindset.
```

Cards:

| Name | Title | Body |
|---|---|---|
| Hikvision ecosystem | Camera and building-security hardware compatibility | Compatible hardware and integration partners help Mistyislet fit into real buildings without forcing a rip-and-replace project. |
| Integrator network | Deployment and on-site commissioning support | Deployment partners help plan wiring, reader placement, gateway commissioning, and handover for each site. |
| API partners | Identity, HR, and visitor workflow integrations | APIs and webhooks connect access events with identity, HR, visitor, and building-operation workflows. |
| Building operators | Access-control rollout and operations feedback | Operators shape the roadmap around rollout speed, auditability, tenant experience, and daily operations. |

## Pricing

Eyebrow:

```text
Pricing
```

Heading:

```text
Plans for pilots, buildings, and multi-site operations
```

### Pilot

Price:

```text
Custom /quote
```

Description:

```text
Validate one site with core cloud access, mobile credentials, and gateway setup.
```

Features:

- Cloud console
- Reader and gateway setup
- Mobile credential pilot

CTA:

```text
Preview
```

### Building

Badge:

```text
Popular
```

Price:

```text
€29 /door/mo
```

Description:

```text
Operate doors, users, groups, schedules, and real-time events for one property.
```

Features:

- Doors, users, schedules
- Live event search
- Admin roles and audit trails
- API/webhook basics
- Support for launch

CTA:

```text
Preview
```

### Enterprise

Price:

```text
Custom
```

Description:

```text
Multi-site controls, integration support, dedicated onboarding, and security review.
```

Features:

- Multi-site governance
- Integration support
- Security review
- Dedicated onboarding
- Custom hardware rollout

CTA:

```text
Preview
```

## FAQ

Heading:

```text
Questions before deployment
```

Items:

| Question | Answer |
|---|---|
| Can Mistyislet work with existing readers and controllers? | Mistyislet is designed for modern reader and gateway deployments, with integration planning for compatible existing building hardware. |
| Does the edge gateway keep doors working offline? | Yes. The edge gateway keeps local access rules and event queues so doors can continue working during network interruptions. |
| Which mobile credentials are supported? | Mobile passes can be issued for NFC, BLE, and QR workflows, alongside cards where a site still needs them. |
| Can we integrate with HR, SSO, or SCIM? | Yes. APIs and webhooks connect access events and users with HR, identity, visitor, and operations systems; SSO and SCIM can be scoped for enterprise rollouts. |
| How are access events audited? | Door events, credential usage, admin changes, and revocations are stored in the cloud console for search and review. |
| How do pricing and hardware deployment work? | Pricing is quoted by site size, door count, hardware scope, integrations, and support needs so pilots can start small. |

## Final CTA

Heading:

```text
Preview the access-control stack
```

Body:

```text
See how cloud rules, edge gateways, readers, and mobile credentials work together.
```

CTA:

```text
Preview
```

## Footer

Brand:

```text
Mistyislet
```

Body:

```text
Cloud access control for modern places.
```

Copyright:

```text
© 2026 Mistyislet | All rights reserved.
```

Navigation:

```text
Product / Solutions / Resources / Partners / Pricing
```
